/*
 * This file was generated automatically by ExtUtils::ParseXS version 3.18 from the
 * contents of Slurmdb.xs. Do not edit this file, edit Slurmdb.xs instead.
 *
 *    ANY CHANGES MADE HERE WILL BE LOST!
 *
 */

#line 1 "Slurmdb.xs"
#include "EXTERN.h"
#include "perl.h"
#include "XSUB.h"

#include "ppport.h"

#include <slurm/slurm.h>
#include <slurm/slurmdb.h>
#include "slurmdb-perl.h"

#include "const-c.inc"

extern void *slurm_xmalloc(size_t, const char *, int, const char *);
extern void slurmdb_destroy_assoc_cond(void *object);
extern void slurmdb_destroy_cluster_cond(void *object);
extern void slurmdb_destroy_job_cond(void *object);
extern void slurmdb_destroy_user_cond(void *object);


#line 30 "Slurmdb.c"
#ifndef PERL_UNUSED_VAR
#  define PERL_UNUSED_VAR(var) if (0) var = var
#endif

#ifndef dVAR
#  define dVAR		dNOOP
#endif


/* This stuff is not part of the API! You have been warned. */
#ifndef PERL_VERSION_DECIMAL
#  define PERL_VERSION_DECIMAL(r,v,s) (r*1000000 + v*1000 + s)
#endif
#ifndef PERL_DECIMAL_VERSION
#  define PERL_DECIMAL_VERSION \
	  PERL_VERSION_DECIMAL(PERL_REVISION,PERL_VERSION,PERL_SUBVERSION)
#endif
#ifndef PERL_VERSION_GE
#  define PERL_VERSION_GE(r,v,s) \
	  (PERL_DECIMAL_VERSION >= PERL_VERSION_DECIMAL(r,v,s))
#endif
#ifndef PERL_VERSION_LE
#  define PERL_VERSION_LE(r,v,s) \
	  (PERL_DECIMAL_VERSION <= PERL_VERSION_DECIMAL(r,v,s))
#endif

/* XS_INTERNAL is the explicit static-linkage variant of the default
 * XS macro.
 *
 * XS_EXTERNAL is the same as XS_INTERNAL except it does not include
 * "STATIC", ie. it exports XSUB symbols. You probably don't want that
 * for anything but the BOOT XSUB.
 *
 * See XSUB.h in core!
 */


/* TODO: This might be compatible further back than 5.10.0. */
#if PERL_VERSION_GE(5, 10, 0) && PERL_VERSION_LE(5, 15, 1)
#  undef XS_EXTERNAL
#  undef XS_INTERNAL
#  if defined(__CYGWIN__) && defined(USE_DYNAMIC_LOADING)
#    define XS_EXTERNAL(name) __declspec(dllexport) XSPROTO(name)
#    define XS_INTERNAL(name) STATIC XSPROTO(name)
#  endif
#  if defined(__SYMBIAN32__)
#    define XS_EXTERNAL(name) EXPORT_C XSPROTO(name)
#    define XS_INTERNAL(name) EXPORT_C STATIC XSPROTO(name)
#  endif
#  ifndef XS_EXTERNAL
#    if defined(HASATTRIBUTE_UNUSED) && !defined(__cplusplus)
#      define XS_EXTERNAL(name) void name(pTHX_ CV* cv __attribute__unused__)
#      define XS_INTERNAL(name) STATIC void name(pTHX_ CV* cv __attribute__unused__)
#    else
#      ifdef __cplusplus
#        define XS_EXTERNAL(name) extern "C" XSPROTO(name)
#        define XS_INTERNAL(name) static XSPROTO(name)
#      else
#        define XS_EXTERNAL(name) XSPROTO(name)
#        define XS_INTERNAL(name) STATIC XSPROTO(name)
#      endif
#    endif
#  endif
#endif

/* perl >= 5.10.0 && perl <= 5.15.1 */


/* The XS_EXTERNAL macro is used for functions that must not be static
 * like the boot XSUB of a module. If perl didn't have an XS_EXTERNAL
 * macro defined, the best we can do is assume XS is the same.
 * Dito for XS_INTERNAL.
 */
#ifndef XS_EXTERNAL
#  define XS_EXTERNAL(name) XS(name)
#endif
#ifndef XS_INTERNAL
#  define XS_INTERNAL(name) XS(name)
#endif

/* Now, finally, after all this mess, we want an ExtUtils::ParseXS
 * internal macro that we're free to redefine for varying linkage due
 * to the EXPORT_XSUB_SYMBOLS XS keyword. This is internal, use
 * XS_EXTERNAL(name) or XS_INTERNAL(name) in your code if you need to!
 */

#undef XS_EUPXS
#if defined(PERL_EUPXS_ALWAYS_EXPORT)
#  define XS_EUPXS(name) XS_EXTERNAL(name)
#else
   /* default to internal */
#  define XS_EUPXS(name) XS_INTERNAL(name)
#endif

#ifndef PERL_ARGS_ASSERT_CROAK_XS_USAGE
#define PERL_ARGS_ASSERT_CROAK_XS_USAGE assert(cv); assert(params)

/* prototype to pass -Wmissing-prototypes */
STATIC void
S_croak_xs_usage(pTHX_ const CV *const cv, const char *const params);

STATIC void
S_croak_xs_usage(pTHX_ const CV *const cv, const char *const params)
{
    const GV *const gv = CvGV(cv);

    PERL_ARGS_ASSERT_CROAK_XS_USAGE;

    if (gv) {
        const char *const gvname = GvNAME(gv);
        const HV *const stash = GvSTASH(gv);
        const char *const hvname = stash ? HvNAME(stash) : NULL;

        if (hvname)
            Perl_croak(aTHX_ "Usage: %s::%s(%s)", hvname, gvname, params);
        else
            Perl_croak(aTHX_ "Usage: %s(%s)", gvname, params);
    } else {
        /* Pants. I don't think that it should be possible to get here. */
        Perl_croak(aTHX_ "Usage: CODE(0x%"UVxf")(%s)", PTR2UV(cv), params);
    }
}
#undef  PERL_ARGS_ASSERT_CROAK_XS_USAGE

#ifdef PERL_IMPLICIT_CONTEXT
#define croak_xs_usage(a,b)    S_croak_xs_usage(aTHX_ a,b)
#else
#define croak_xs_usage        S_croak_xs_usage
#endif

#endif

/* NOTE: the prototype of newXSproto() is different in versions of perls,
 * so we define a portable version of newXSproto()
 */
#ifdef newXS_flags
#define newXSproto_portable(name, c_impl, file, proto) newXS_flags(name, c_impl, file, proto, 0)
#else
#define newXSproto_portable(name, c_impl, file, proto) (PL_Sv=(SV*)newXS(name, c_impl, file), sv_setpv(PL_Sv, proto), (CV*)PL_Sv)
#endif /* !defined(newXS_flags) */

#line 172 "Slurmdb.c"

/* INCLUDE:  Including 'const-xs.inc' from 'Slurmdb.xs' */


XS_EUPXS(XS_Slurmdb_constant); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Slurmdb_constant)
{
    dVAR; dXSARGS;
    if (items != 1)
       croak_xs_usage(cv,  "sv");
    PERL_UNUSED_VAR(ax); /* -Wall */
    SP -= items;
    {
#line 4 "./const-xs.inc"
#ifdef dXSTARG
	dXSTARG; /* Faster if we have it.  */
#else
	dTARGET;
#endif
	STRLEN		len;
        int		type;
	IV		iv;
	/* NV		nv;	Uncomment this if you need to return NVs */
	/* const char	*pv;	Uncomment this if you need to return PVs */
#line 197 "Slurmdb.c"
	SV *	sv = ST(0)
;
	const char *	s = SvPV(sv, len);
#line 18 "./const-xs.inc"
        /* Change this to constant(aTHX_ s, len, &iv, &nv);
           if you need to return both NVs and IVs */
	type = constant(aTHX_ s, len, &iv);
      /* Return 1 or 2 items. First is error message, or undef if no error.
           Second, if present, is found value */
        switch (type) {
        case PERL_constant_NOTFOUND:
          sv =
	    sv_2mortal(newSVpvf("%s is not a valid Slurmdb macro", s));
          PUSHs(sv);
          break;
        case PERL_constant_NOTDEF:
          sv = sv_2mortal(newSVpvf(
	    "Your vendor has not defined Slurmdb macro %s, used",
				   s));
          PUSHs(sv);
          break;
        case PERL_constant_ISIV:
          EXTEND(SP, 1);
          PUSHs(&PL_sv_undef);
          PUSHi(iv);
          break;
	/* Uncomment this if you need to return NOs
        case PERL_constant_ISNO:
          EXTEND(SP, 1);
          PUSHs(&PL_sv_undef);
          PUSHs(&PL_sv_no);
          break; */
	/* Uncomment this if you need to return NVs
        case PERL_constant_ISNV:
          EXTEND(SP, 1);
          PUSHs(&PL_sv_undef);
          PUSHn(nv);
          break; */
	/* Uncomment this if you need to return PVs
        case PERL_constant_ISPV:
          EXTEND(SP, 1);
          PUSHs(&PL_sv_undef);
          PUSHp(pv, strlen(pv));
          break; */
	/* Uncomment this if you need to return PVNs
        case PERL_constant_ISPVN:
          EXTEND(SP, 1);
          PUSHs(&PL_sv_undef);
          PUSHp(pv, iv);
          break; */
	/* Uncomment this if you need to return SVs
        case PERL_constant_ISSV:
          EXTEND(SP, 1);
          PUSHs(&PL_sv_undef);
          PUSHs(sv);
          break; */
	/* Uncomment this if you need to return UNDEFs
        case PERL_constant_ISUNDEF:
          break; */
	/* Uncomment this if you need to return UVs
        case PERL_constant_ISUV:
          EXTEND(SP, 1);
          PUSHs(&PL_sv_undef);
          PUSHu((UV)iv);
          break; */
	/* Uncomment this if you need to return YESs
        case PERL_constant_ISYES:
          EXTEND(SP, 1);
          PUSHs(&PL_sv_undef);
          PUSHs(&PL_sv_yes);
          break; */
        default:
          sv = sv_2mortal(newSVpvf(
	    "Unexpected return type %d while processing Slurmdb macro %s, used",
               type, s));
          PUSHs(sv);
        }
#line 275 "Slurmdb.c"
	PUTBACK;
	return;
    }
}


/* INCLUDE: Returning to 'Slurmdb.xs' from 'const-xs.inc' */


XS_EUPXS(XS_Slurmdb_connection_get); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Slurmdb_connection_get)
{
    dVAR; dXSARGS;
    if (items != 0)
       croak_xs_usage(cv,  "");
    {
	void *	RETVAL;
	dXSTARG;

	RETVAL = slurmdb_connection_get();
	XSprePUSH; PUSHi(PTR2IV(RETVAL));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Slurmdb_connection_close); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Slurmdb_connection_close)
{
    dVAR; dXSARGS;
    if (items != 1)
       croak_xs_usage(cv,  "db_conn");
    {
	void*	db_conn = INT2PTR(void *,SvIV(ST(0)))
;
	int	RETVAL;
	dXSTARG;

	RETVAL = slurmdb_connection_close(db_conn);
	XSprePUSH; PUSHi((IV)RETVAL);
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Slurmdb_clusters_get); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Slurmdb_clusters_get)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "db_conn, conditions");
    {
	void*	db_conn = INT2PTR(void *,SvIV(ST(0)))
;
	HV*	conditions;
	SV *	RETVAL;

	STMT_START {
		SV* const xsub_tmp_sv = ST(1);
		SvGETMAGIC(xsub_tmp_sv);
		if (SvROK(xsub_tmp_sv) && SvTYPE(SvRV(xsub_tmp_sv)) == SVt_PVHV){
		    conditions = (HV*)SvRV(xsub_tmp_sv);
		}
		else{
		    Perl_croak(aTHX_ "%s: %s is not a HASH reference",
				"Slurmdb::clusters_get",
				"conditions");
		}
	} STMT_END
;
#line 37 "Slurmdb.xs"
	AV*   results;
	HV*   rh;
	List  list = NULL;
	ListIterator itr;
	slurmdb_cluster_cond_t *cluster_cond = (slurmdb_cluster_cond_t*)
		slurm_xmalloc(sizeof(slurmdb_cluster_cond_t), __FILE__,
		__LINE__, "slurmdb_clusters_get");
	slurmdb_init_cluster_cond(cluster_cond, 0);
	slurmdb_cluster_rec_t *rec = NULL;

	if (hv_to_cluster_cond(conditions, cluster_cond) < 0) {
		XSRETURN_UNDEF;
	}
	results = (AV*)sv_2mortal((SV*)newAV());
#line 361 "Slurmdb.c"
#line 52 "Slurmdb.xs"
	list = slurmdb_clusters_get(db_conn, cluster_cond);
	if (list) {
	    itr = slurm_list_iterator_create(list);

	    while ((rec = slurm_list_next(itr))) {
		rh = (HV *)sv_2mortal((SV*)newHV());
		if (cluster_rec_to_hv(rec, rh) < 0) {
		    XSRETURN_UNDEF;
		}
		av_push(results, newRV((SV*)rh));
	    }
	    slurm_list_destroy(list);
	}
	RETVAL = newRV((SV*)results);
	slurmdb_destroy_cluster_cond(cluster_cond);
#line 378 "Slurmdb.c"
	ST(0) = RETVAL;
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Slurmdb_report_cluster_account_by_user); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Slurmdb_report_cluster_account_by_user)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "db_conn, assoc_condition");
    {
	void*	db_conn = INT2PTR(void *,SvIV(ST(0)))
;
	HV*	assoc_condition;
	SV *	RETVAL;

	STMT_START {
		SV* const xsub_tmp_sv = ST(1);
		SvGETMAGIC(xsub_tmp_sv);
		if (SvROK(xsub_tmp_sv) && SvTYPE(SvRV(xsub_tmp_sv)) == SVt_PVHV){
		    assoc_condition = (HV*)SvRV(xsub_tmp_sv);
		}
		else{
		    Perl_croak(aTHX_ "%s: %s is not a HASH reference",
				"Slurmdb::report_cluster_account_by_user",
				"assoc_condition");
		}
	} STMT_END
;
#line 75 "Slurmdb.xs"
	AV*   results;
	List  list = NULL;
	slurmdb_assoc_cond_t *assoc_cond = (slurmdb_assoc_cond_t*)
		slurm_xmalloc(sizeof(slurmdb_assoc_cond_t), __FILE__,
		__LINE__, "slurmdb_report_cluster_account_by_user");

	if (hv_to_assoc_cond(assoc_condition, assoc_cond) < 0) {
		XSRETURN_UNDEF;
	}
	results = (AV*)sv_2mortal((SV*)newAV());
#line 422 "Slurmdb.c"
#line 86 "Slurmdb.xs"
	list = slurmdb_report_cluster_account_by_user(db_conn, assoc_cond);
	if (list) {
	    if (report_cluster_rec_list_to_av(list, results) < 0) {
		XSRETURN_UNDEF;
	    }
	    slurm_list_destroy(list);
	}
	RETVAL = newRV((SV*)results);
	slurmdb_destroy_assoc_cond(assoc_cond);
#line 433 "Slurmdb.c"
	ST(0) = RETVAL;
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Slurmdb_report_cluster_user_by_account); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Slurmdb_report_cluster_user_by_account)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "db_conn, assoc_condition");
    {
	void*	db_conn = INT2PTR(void *,SvIV(ST(0)))
;
	HV*	assoc_condition;
	SV *	RETVAL;

	STMT_START {
		SV* const xsub_tmp_sv = ST(1);
		SvGETMAGIC(xsub_tmp_sv);
		if (SvROK(xsub_tmp_sv) && SvTYPE(SvRV(xsub_tmp_sv)) == SVt_PVHV){
		    assoc_condition = (HV*)SvRV(xsub_tmp_sv);
		}
		else{
		    Perl_croak(aTHX_ "%s: %s is not a HASH reference",
				"Slurmdb::report_cluster_user_by_account",
				"assoc_condition");
		}
	} STMT_END
;
#line 103 "Slurmdb.xs"
	AV*   results;
	List  list = NULL;
	slurmdb_assoc_cond_t *assoc_cond = (slurmdb_assoc_cond_t*)
		slurm_xmalloc(sizeof(slurmdb_assoc_cond_t), __FILE__,
		__LINE__, "slurmdb_report_cluster_user_by_account");

	if (hv_to_assoc_cond(assoc_condition, assoc_cond) < 0) {
		XSRETURN_UNDEF;
	}
	results = (AV*)sv_2mortal((SV*)newAV());
#line 477 "Slurmdb.c"
#line 114 "Slurmdb.xs"
	list = slurmdb_report_cluster_user_by_account(db_conn, assoc_cond);
	if (list) {
	    if (report_cluster_rec_list_to_av(list, results) < 0) {
		XSRETURN_UNDEF;
	    }
	    slurm_list_destroy(list);
	}
	RETVAL = newRV((SV*)results);
	slurmdb_destroy_assoc_cond(assoc_cond);
#line 488 "Slurmdb.c"
	ST(0) = RETVAL;
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Slurmdb_report_job_sizes_grouped_by_top_account); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Slurmdb_report_job_sizes_grouped_by_top_account)
{
    dVAR; dXSARGS;
    if (items != 4)
       croak_xs_usage(cv,  "db_conn, job_condition, grouping_array, flat_view");
    {
	void*	db_conn = INT2PTR(void *,SvIV(ST(0)))
;
	HV*	job_condition;
	AV*	grouping_array;
	bool	flat_view = (bool)SvTRUE(ST(3))
;
	SV *	RETVAL;

	STMT_START {
		SV* const xsub_tmp_sv = ST(1);
		SvGETMAGIC(xsub_tmp_sv);
		if (SvROK(xsub_tmp_sv) && SvTYPE(SvRV(xsub_tmp_sv)) == SVt_PVHV){
		    job_condition = (HV*)SvRV(xsub_tmp_sv);
		}
		else{
		    Perl_croak(aTHX_ "%s: %s is not a HASH reference",
				"Slurmdb::report_job_sizes_grouped_by_top_account",
				"job_condition");
		}
	} STMT_END
;

	STMT_START {
		SV* const xsub_tmp_sv = ST(2);
		SvGETMAGIC(xsub_tmp_sv);
		if (SvROK(xsub_tmp_sv) && SvTYPE(SvRV(xsub_tmp_sv)) == SVt_PVAV){
		    grouping_array = (AV*)SvRV(xsub_tmp_sv);
		}
		else{
		    Perl_croak(aTHX_ "%s: %s is not an ARRAY reference",
				"Slurmdb::report_job_sizes_grouped_by_top_account",
				"grouping_array");
		}
	} STMT_END
;
#line 133 "Slurmdb.xs"
	AV*   results;
	List  list = NULL;
	List grouping_list = slurm_list_create(NULL);
	slurmdb_job_cond_t *job_cond = (slurmdb_job_cond_t*)
		slurm_xmalloc(sizeof(slurmdb_job_cond_t), __FILE__,
		__LINE__, "slurmdb_report_job_sizes_grouped_by_top_account");
	if (hv_to_job_cond(job_condition, job_cond) < 0) {
		XSRETURN_UNDEF;
	}
	if (av_to_cluster_grouping_list(grouping_array, grouping_list) < 0) {
		XSRETURN_UNDEF;
	}
	results = (AV*)sv_2mortal((SV*)newAV());
#line 552 "Slurmdb.c"
#line 147 "Slurmdb.xs"
	list = slurmdb_report_job_sizes_grouped_by_top_account(db_conn,
		job_cond, grouping_list, flat_view);
	if (list) {
	    if (cluster_grouping_list_to_av(list, results) < 0) {
		XSRETURN_UNDEF;
	    }
	    slurm_list_destroy(list);
	}
	RETVAL = newRV((SV*)results);
	slurmdb_destroy_job_cond(job_cond);
	slurm_list_destroy(grouping_list);
#line 565 "Slurmdb.c"
	ST(0) = RETVAL;
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Slurmdb_report_user_top_usage); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Slurmdb_report_user_top_usage)
{
    dVAR; dXSARGS;
    if (items != 3)
       croak_xs_usage(cv,  "db_conn, user_condition, group_accounts");
    {
	void*	db_conn = INT2PTR(void *,SvIV(ST(0)))
;
	HV*	user_condition;
	bool	group_accounts = (bool)SvTRUE(ST(2))
;
	SV *	RETVAL;

	STMT_START {
		SV* const xsub_tmp_sv = ST(1);
		SvGETMAGIC(xsub_tmp_sv);
		if (SvROK(xsub_tmp_sv) && SvTYPE(SvRV(xsub_tmp_sv)) == SVt_PVHV){
		    user_condition = (HV*)SvRV(xsub_tmp_sv);
		}
		else{
		    Perl_croak(aTHX_ "%s: %s is not a HASH reference",
				"Slurmdb::report_user_top_usage",
				"user_condition");
		}
	} STMT_END
;
#line 167 "Slurmdb.xs"
	AV*   results;
	List  list = NULL;
	slurmdb_user_cond_t* user_cond = (slurmdb_user_cond_t*)
		slurm_xmalloc(sizeof(slurmdb_user_cond_t), __FILE__,
		__LINE__, "slurmdb_report_user_top_usage");
	user_cond->assoc_cond =	(slurmdb_assoc_cond_t*)
		slurm_xmalloc(sizeof(slurmdb_assoc_cond_t), __FILE__,
		__LINE__, "slurmdb_report_user_top_usage");
	if (hv_to_user_cond(user_condition, user_cond) < 0) {
		XSRETURN_UNDEF;
	}
	results = (AV*)sv_2mortal((SV*)newAV());
#line 613 "Slurmdb.c"
#line 180 "Slurmdb.xs"
	list = slurmdb_report_user_top_usage(db_conn, user_cond,
					     group_accounts);
	if (list) {
	    if (report_cluster_rec_list_to_av(list, results) < 0) {
		XSRETURN_UNDEF;
	    }
	    slurm_list_destroy(list);
	}
	RETVAL = newRV((SV*)results);
	slurmdb_destroy_user_cond(user_cond);
#line 625 "Slurmdb.c"
	ST(0) = RETVAL;
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Slurmdb_jobs_get); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Slurmdb_jobs_get)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "db_conn, conditions");
    {
	void*	db_conn = INT2PTR(void *,SvIV(ST(0)))
;
	HV*	conditions;
	SV *	RETVAL;

	STMT_START {
		SV* const xsub_tmp_sv = ST(1);
		SvGETMAGIC(xsub_tmp_sv);
		if (SvROK(xsub_tmp_sv) && SvTYPE(SvRV(xsub_tmp_sv)) == SVt_PVHV){
		    conditions = (HV*)SvRV(xsub_tmp_sv);
		}
		else{
		    Perl_croak(aTHX_ "%s: %s is not a HASH reference",
				"Slurmdb::jobs_get",
				"conditions");
		}
	} STMT_END
;
#line 198 "Slurmdb.xs"
	AV*   results;
	HV*   rh;
	List  list = NULL;
	ListIterator itr;
	slurmdb_job_cond_t *job_cond = (slurmdb_job_cond_t*)
		slurm_xmalloc(sizeof(slurmdb_job_cond_t), __FILE__,
		__LINE__, "slurmdb_jobs_get");
	slurmdb_job_rec_t *rec = NULL;

	if (hv_to_job_cond(conditions, job_cond) < 0) {
		XSRETURN_UNDEF;
	}
	results = (AV*)sv_2mortal((SV*)newAV());
#line 672 "Slurmdb.c"
#line 212 "Slurmdb.xs"
	list = slurmdb_jobs_get(db_conn, job_cond);
	if (list) {
	    itr = slurm_list_iterator_create(list);

	    while ((rec = slurm_list_next(itr))) {
		rh = (HV *)sv_2mortal((SV*)newHV());
		if (job_rec_to_hv(rec, rh) < 0) {
		    XSRETURN_UNDEF;
		}
		av_push(results, newRV((SV*)rh));
	    }
	    slurm_list_destroy(list);
	}
	RETVAL = newRV((SV*)results);
	slurmdb_destroy_job_cond(job_cond);
#line 689 "Slurmdb.c"
	ST(0) = RETVAL;
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Slurmdb_qos_get); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Slurmdb_qos_get)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "db_conn, conditions");
    {
	void*	db_conn = INT2PTR(void *,SvIV(ST(0)))
;
	HV*	conditions;
	SV *	RETVAL;

	STMT_START {
		SV* const xsub_tmp_sv = ST(1);
		SvGETMAGIC(xsub_tmp_sv);
		if (SvROK(xsub_tmp_sv) && SvTYPE(SvRV(xsub_tmp_sv)) == SVt_PVHV){
		    conditions = (HV*)SvRV(xsub_tmp_sv);
		}
		else{
		    Perl_croak(aTHX_ "%s: %s is not a HASH reference",
				"Slurmdb::qos_get",
				"conditions");
		}
	} STMT_END
;
#line 236 "Slurmdb.xs"
	AV*   results;
	HV*   rh;
	List  list = NULL, all = NULL;
	ListIterator itr;
	slurmdb_qos_cond_t *qos_cond = (slurmdb_qos_cond_t*)
		slurm_xmalloc(sizeof(slurmdb_qos_cond_t), __FILE__,
		__LINE__, "slurmdb_qos_get");
	slurmdb_qos_rec_t *rec = NULL;

	if (hv_to_qos_cond(conditions, qos_cond) < 0) {
		XSRETURN_UNDEF;
	}
	results = (AV*)sv_2mortal((SV*)newAV());
#line 736 "Slurmdb.c"
#line 250 "Slurmdb.xs"
	list = slurmdb_qos_get(db_conn, qos_cond);
	all = slurmdb_qos_get(db_conn, NULL);
	if (list) {
	    itr = slurm_list_iterator_create(list);

	    while ((rec = slurm_list_next(itr))) {
		rh = (HV *)sv_2mortal((SV*)newHV());
		if (qos_rec_to_hv(rec, rh, all) < 0) {
		    XSRETURN_UNDEF;
		}
		av_push(results, newRV((SV*)rh));
	    }
	    slurm_list_destroy(list);
	}
	RETVAL = newRV((SV*)results);
	slurmdb_destroy_qos_cond(qos_cond);
#line 754 "Slurmdb.c"
	ST(0) = RETVAL;
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Slurmdb_find_tres_count_in_string); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Slurmdb_find_tres_count_in_string)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "tres_str_in, id");
    {
	char *	tres_str_in = (char *)SvPV_nolen(ST(0))
;
	int	id = (int)SvIV(ST(1))
;
	UV	RETVAL;
	dXSTARG;

	RETVAL = slurmdb_find_tres_count_in_string(tres_str_in, id);
	XSprePUSH; PUSHu((UV)RETVAL);
    }
    XSRETURN(1);
}

#ifdef __cplusplus
extern "C"
#endif
XS_EXTERNAL(boot_Slurmdb); /* prototype to pass -Wmissing-prototypes */
XS_EXTERNAL(boot_Slurmdb)
{
    dVAR; dXSARGS;
#if (PERL_REVISION == 5 && PERL_VERSION < 9)
    char* file = __FILE__;
#else
    const char* file = __FILE__;
#endif

    PERL_UNUSED_VAR(cv); /* -W */
    PERL_UNUSED_VAR(items); /* -W */
#ifdef XS_APIVERSION_BOOTCHECK
    XS_APIVERSION_BOOTCHECK;
#endif
    XS_VERSION_BOOTCHECK;

        (void)newXSproto_portable("Slurmdb::constant", XS_Slurmdb_constant, file, "$");
        (void)newXSproto_portable("Slurmdb::connection_get", XS_Slurmdb_connection_get, file, "");
        (void)newXSproto_portable("Slurmdb::connection_close", XS_Slurmdb_connection_close, file, "$");
        (void)newXSproto_portable("Slurmdb::clusters_get", XS_Slurmdb_clusters_get, file, "$$");
        (void)newXSproto_portable("Slurmdb::report_cluster_account_by_user", XS_Slurmdb_report_cluster_account_by_user, file, "$$");
        (void)newXSproto_portable("Slurmdb::report_cluster_user_by_account", XS_Slurmdb_report_cluster_user_by_account, file, "$$");
        (void)newXSproto_portable("Slurmdb::report_job_sizes_grouped_by_top_account", XS_Slurmdb_report_job_sizes_grouped_by_top_account, file, "$$$$");
        (void)newXSproto_portable("Slurmdb::report_user_top_usage", XS_Slurmdb_report_user_top_usage, file, "$$$");
        (void)newXSproto_portable("Slurmdb::jobs_get", XS_Slurmdb_jobs_get, file, "$$");
        (void)newXSproto_portable("Slurmdb::qos_get", XS_Slurmdb_qos_get, file, "$$");
        (void)newXSproto_portable("Slurmdb::find_tres_count_in_string", XS_Slurmdb_find_tres_count_in_string, file, "$$");
#if (PERL_REVISION == 5 && PERL_VERSION >= 9)
  if (PL_unitcheckav)
       call_list(PL_scopestack_ix, PL_unitcheckav);
#endif
    XSRETURN_YES;
}

